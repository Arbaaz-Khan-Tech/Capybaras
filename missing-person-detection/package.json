{
  "name": "missing-person-detection",
  "version": "1.0.0",
  "description": "A Missing Person Detection system with Aadhar integration.",
  "main": "src/index.tsx",
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && set PORT=3001 && react-scripts start",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "test": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "autoprefixer": "^10.4.2",
    "postcss": "^8.4.5",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.3.4",
    "react-scripts": "4.0.3",
    "tailwindcss": "^2.2.19"
  },
  "devDependencies": {
    "typescript": "^4.4.4"
  },
  "keywords": [
    "missing",
    "person",
    "detection",
    "aadhar",
    "react",
    "tailwindcss"
  ],
  "author": "Your Name",
  "license": "MIT",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
